[defaults]
action_warnings = True
inventory = inventory.yml
roles_path = roles
host_key_checking = True    
remote_user = ansible
private_key_file = ~/.ssh/lab-ansible
forks = 10
timeout = 30

[ssh_connection]
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s