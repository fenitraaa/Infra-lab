apiVersion: v1
kind: Service
metadata:
  name: preserve-api-stable
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: preserve-api
    version: stable
  ports:
  - port: 8080
    targetPort: 8080
